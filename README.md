# Git Examples

In this repo we exercise some git commands.

## Git Commands

Here are the commands we practice:

```bash
git init
git add filename
git add --all
git commit -m "message"
git commit -a
git config --global user.name "username"
git config --global user.gmail "username@gmail.com"
git remote add origin url
git push -u origin master
git clone url
git pull
git status
```